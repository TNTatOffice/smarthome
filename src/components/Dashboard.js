import React from 'react'


export default function Dashboard(props) {

    return (
        <div>
            <h1 className="heading">{props.title}</h1>
            <div className="dashboard">
            <h2 className="addRoom">Click to a Room</h2>  
            
            </div>

        </div>
    )
}
